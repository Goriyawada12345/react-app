// src/components/MainContent.js
import React from 'react';

const MainContent = () => {
    return (
        <main>
            <h2>Main Article</h2>
            <p>
                This is the main content area where the article or the primary content of the page goes. 
                HTML5 introduces new elements that allow you to structure your web pages in a more meaningful way.
            </p>
            <p>
                This main section can have multiple paragraphs, images, and other elements that represent the core content of the page.
            </p>
        </main>
    );
};

export default MainContent;
